{
  "name": "app",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "reset": "git reset --hard HEAD"
  },
  "dependencies": {
    "node-zookeeper-client": "^1.1.3"
  },
  "devDependencies": {
    "@types/node-zookeeper-client": "^1.1.0",
    "@types/node": "latest",
    "typescript": "latest"
  }
}
